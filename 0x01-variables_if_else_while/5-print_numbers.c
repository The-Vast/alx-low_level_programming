#include <stdio.h>

/**
 * main - where all the codes are cooked
 * Return: Always 0 (Success)
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);
	printf('\n');

	return (0);
}
