#include <stdio.h>

/**
 * main - where all the codes are cooked
 * Return: Always 0 (Success)
 */

int main(void)
{
	long int num;

	for (num = 0; num < 10; num++)
		putchar(num);
	putchar('\n');

	return (0);
}
