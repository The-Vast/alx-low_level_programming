#include <stdio.h>
#include <stdlib.h>

/**
 * main - where all the codes are cooked
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		putchar(n + '0');
	putchar('\n');

	return (0);
}
