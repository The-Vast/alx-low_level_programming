#include "main.h"

/**
 * _abs - prints absolute value of integer
 * @n: The integer to be checked
 *
 * Return: absolute value is returned
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
